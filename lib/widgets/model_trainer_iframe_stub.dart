import 'package:flutter/material.dart';

Widget buildModelTrainerIframe() {
  return const Center(
    child: Text(
      'Embedded Teachable Machine Trainer is available in Web browser mode.',
      style: TextStyle(fontSize: 16),
    ),
  );
}
